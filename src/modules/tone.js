import 'tone/build/Tone.js';

export const {
    Transport,
    Player,
    Buffer,
    CrossFade,
    Loop,
    Sequence,
} = window.Tone;

export const { Tone } = window;

export default window.Tone;
